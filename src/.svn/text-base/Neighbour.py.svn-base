'''
Contains a matchset neighbouring to a particular matchset with its corresponding compatibility value
'''
from Matchset import Matchset

class Neighbour:
    M = Matchset() 
    gamma = 1
    
    